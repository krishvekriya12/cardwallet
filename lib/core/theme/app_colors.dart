import 'package:flutter/material.dart';

abstract final class AppColors {
  AppColors._();

  static const primary = Color(0xFF4C00B0);
  static const primaryLight = Color(0xFF7E42D9);
  static const primaryDark = Color(0xFF330080);
  static const primarySoft = Color(0x1A4C00B0);
  static const splashGradientEnd = Color(0xFF2D1B69);

  static const secondary = Color(0xFFFF6584);
  static const secondaryLight = Color(0xFFFF93AA);
  static const secondaryDark = Color(0xFFCC4466);
  static const secondarySoft = Color(0x1AFF6584);

  static const success = Color(0xFF00D9A5);
  static const successSoft = Color(0x1A00D9A5);

  static const warning = Color(0xFFFF9A3C);
  static const warningSoft = Color(0x1AFF9A3C);

  static const error = Color(0xFFFF4D6A);
  static const errorSoft = Color(0x1AFF4D6A);

  static const info = Color(0xFF3CAEFC);
  static const infoSoft = Color(0x1A3CAEFC);

  static const darkPage = Color(0xFF15121B);

  static const darkSurface = Color(0xFF211E27);

  static const darkElevated = Color(0xFF2C2833);

  static const darkField = Color(0xFF322D3A);

  static const darkBorder = Color(0xFF3F3A4B);

  static const lightPage = Color(0xFFFEF7FF);

  static const lightSurface = Color(0xFFF3EBF8);

  static const lightElevated = Color(0xFFE8DBEF);

  static const lightField = Color(0xFFEDE0F4);

  static const lightBorder = Color(0xFFE2D0ED);

  static const darkTextPrimary = Color(0xFFECE6F0);
  static const darkTextSecondary = Color(0xFF9F94AB);
  static const darkTextTertiary = Color(0xFF756B81);
  static const darkTextDisabled = Color(0xFF4F4657);

  static const lightTextPrimary = Color(0xFF1D1625);
  static const lightTextSecondary = Color(0xFF5A4D6B);
  static const lightTextTertiary = Color(0xFF8C7D9E);
  static const lightTextDisabled = Color(0xFFC0B2CE);

  static const darkShadow = Color(0xFF000000);
  static const lightShadow = Color(0xFF6C63FF);

  static const creditGradient = [Color(0xFF4A00E0), Color(0xFF1CB5E0)];

  static const debitGradient = [Color(0xFF00B09B), Color(0xFF096344)];

  static const pointGradient = [Color(0xFFFF6B35), Color(0xFFC0392B)];

  static const membershipGradient = [Color(0xFF1A237E), Color(0xFF283593)];

  static const couponGradient = [Color(0xFFEC407A), Color(0xFF8E24AA)];

  static const giftGradient = [Color(0xFFFFB300), Color(0xFFE65100)];

  static const eventGradient = [Color(0xFF00897B), Color(0xFF004D40)];

  static const otherGradient = [Color(0xFF5C6BC0), Color(0xFF7B1FA2)];

  static const visaColor = Color(0xFF1A1F71);
  static const mastercardRed = Color(0xFFEB001B);
  static const mastercardOrange = Color(0xFFF79E1B);
  static const amexBlue = Color(0xFF007BC1);
  static const rupayColor = Color(0xFF097CC5);

  static const white = Color(0xFFFFFFFF);
  static const black = Color(0xFF000000);
  static const transparent = Color(0x00000000);

  static const glassWhite = Color(0x14FFFFFF);
  static const glassBorder = Color(0x1EFFFFFF);
}
